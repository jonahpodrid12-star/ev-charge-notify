services:
  - type: web
      name: ev-charge-notify
          runtime: python
              buildCommand: pip install -r requirements.txt
                  startCommand: gunicorn app:app
                      plan: free
